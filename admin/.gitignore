# 依赖
node_modules
dist
dist-ssr

# 环境变量（含真实 Supabase 配置，不提交；用 .env.example 作为模板）
.env
.env.local
.env.*.local
.env.test

# 测试产物（Playwright / Allure，本地生成，不提交）
allure-results
allure-report
test-results
playwright-report

# 日志与系统文件
*.log
.DS_Store
