# 实施说明：时光章节 × 纸与光

> 对应提案页 [proposals.html](./proposals.html) 对比器里的
> **第二批 · 结构 → 新① 时光章节** + **第二批 · 视觉 → 新Ⅰ 纸与光**。
> 本文只讲「怎么落地、要多复杂、动到什么面」，不含方案比较（比较看提案页）。

---

## 一、这套组合是什么

| | 内容 | 性质 |
|---|---|---|
| **结构**（新① 时光章节） | 在列表里按时间插章节头，每章一行小计：`今天 3 件待办 · 已完成 1 件` / `昨天 一起完成 2 件` / `更早 · 九月 一起完成 6 件 · 解锁贴纸 2 张` | 改 JS 渲染 |
| **视觉**（新Ⅰ 纸与光） | 未完成 = 新纸（白、浮起）；已完成 = 晒过的纸（暖一度、压平、内凹）。卡片尺寸、信息、位置全不变 | 改 CSS |

**两条核心约束**（来自你的原话）：卡片是原子，完成的待办不许缩成一行；隐藏款留在原位并被看得更清楚。

---

## 二、改动清单（逐文件）

### A. 纸与光 —— 纯 CSS，1 个文件

全部在 `public/css/style.css`：

| 位置 | 改动 | 说明 |
|---|---|---|
| `.todo`（现 779 行） | 圆角 16→20px、内距 15/16→17/19/16、阴影换双层暖影 + `inset` 白高光、底色改渐变 | 材质升级，尺寸/信息不动 |
| `.todo--done`（现 829 行） | 暖底 `linear-gradient(#fdf6f1, #f9efe8)` + `inset 0 2px 7px` 内凹，去掉投影；文字色转暖灰 | 「晒过的纸」 |
| `.todo__check`（现 875 行） | 18→20px，`opacity` .65→.85 | 未完成态更清楚 |
| `.todo--done .todo__check`（现 920–934 行） | **从 `opacity:0` 改成可见的撤销入口** | 顺手修现网缺陷：现在「取消完成」在界面上根本不存在，只能长按找菜单 |
| `.container`（现 462 行） | 顶部一层柔光 | 让卡片浮在光上 |

**⚠️ 两个级联陷阱（必须显式处理，否则线上会静默变样）：**

1. **稀有度会被吃掉**。`.todo--rare / --epic / --legendary` 在 2821 / 2828 / 2836 行，靠「排在 `.todo--done` 后面」取胜（都是单类选择器，后者胜）。新增的 `.todo` / `.todo--done` 若追加到文件尾部（4000+ 行），**级联上就赢过稀有度 → 线上 7 张隐藏款的渐变底全部变白**。
   → 做法：新增规则插在 2821 行之前；**更稳的做法**是把稀有度选择器提升为 `.todo.todo--rare`（权重 0,2,0），从此不怕后人追加。
2. **置顶 + 已完成 的组合**：`.todo--pinned`（847 行）现在排在 `.todo--done` 之后，所以「置顶且已完成」的卡片背景由置顶渐变赢。新增 `.todo--done` 若放到 847 行之后会反转这个行为 —— 要么保持原行为（放在 847 之前），要么改并说明。

**另：颜色要收敛为 token**。提案页里写的是硬编码值（`#fdf6f1` / `#8c7782` 等），实施时按仓库规则（AGENTS.md「组件只引用 semantic 层」）落到 `:root`：能复用 `--cream-*` / `--stone-*` 就复用，不足再补 1–2 个语义 token（如 `--color-surface-sunned`）。

**复杂度：S** ｜ 约 70 行 CSS、1 个文件 ｜ 工时 **2–3 小时**（含真机目测）｜ 无逻辑风险

### B. 时光章节 —— JS，2 个文件 + 1 个新纯函数

| 文件 | 改动 |
|---|---|
| `public/js/timeline.js`（**新增**） | 纯函数：`groupByLocalPeriod(todos, now)` → `[{ key, label, subtitle, todos }]`；`localDayKey(iso)` / `localMonthKey(iso)`；小计计算（件数 + 当月解锁贴纸数） |
| `public/js/app.js` | `render()`（1261 行）里把章节头与 todo 一起排进同一个 `DocumentFragment`；新增 `renderChapterHeader` / `updateChapterHeader`（Map 复用，避免重建与重播入场动画）；章节内套用分组顺序 |
| `public/css/style.css` | 章节头样式（`.tl-chap` 及其内部），约 30 行 |
| `state.js` | **不动**。`sortTodos`（152 行）保持原样：未完成仍在前 |

**⚠️ 头号实现坑（这是本方案最容易写错的地方）：**
`render()` 是增量渲染 —— 1325 行先用 `.todo[data-id]` 收集现有节点，1339 行把它们全塞进一个 fragment，最后 **1355 行 `todoListEl.appendChild(frag)` 把整个 fragment 移到容器末尾**。
如果章节头只是「插进去」而不参与这次搬迁，**第二次 render 之后所有章节头会挤到列表顶部，所有 todo 跑到下面** —— 看起来就是「章节全错位」。
→ 做法：章节头节点也必须按顺序 `appendChild` 进同一个 fragment（章节头用 `<li class="tl-chap">`、**不带 `data-id`**，所以 1325 行的 existing Map 天然忽略它，不会被删除逻辑误伤）。

**⚠️ 第二个坑：时区。** `completed_at` 是 `timestamptz`（UTC）。分组必须按**设备本地时间**取年月日：
用 `d.getFullYear() / getMonth() / getDate()`，**绝不能用 `toISOString().slice(0,10)`** —— 那会按 UTC 切天，北京时间晚上 8 点之后完成的事会被算成"昨天"。

**复杂度：M** ｜ 约 150 行（含新文件）｜ 工时 **4–6 小时**（含自测）｜ 三处风险：重排错位、时区日界、章节内排序

### C. 合计

**一次发布约 1 天**；建议拆成两次（见第六节）。无新依赖、无新字体、无原生代码。

---

## 三、SQL：**不需要**（逐条论证）

| 需要的数据 | 现状 | 结论 |
|---|---|---|
| `completed_at`（分月/分天） | `todos` 表已有该列 | ✅ 不用加 |
| `completed_by`（谁完成的） | 已有 | ✅ |
| `rarity`（隐藏款渐变） | 已有 | ✅ |
| `unlocked_at`（当月解锁几张贴纸） | `stickers` 表已有（`migration-blindbox-stickers.sql:45`） | ✅ 不用加 |
| 取数范围 | `db.js:listTodos()` 是 `select('*')` + 仅未删除，**一次拿回全部** | ✅ 不用新查询 |
| 索引 | 已有 `idx_todos_pinned_completed_created` | ✅ 够用；63 行规模下按 `completed_at` 聚合走内存，**不要提前加索引** |

**唯一会需要 SQL 的情形**（本方案不涉及）：将来若把归档做成「按需分页从服务端取」，
才需要 `(completed, completed_at DESC) WHERE deleted_at IS NULL` 这类索引。

---

## 四、交付通道：**只走热更新，不需要壳更新**

| 问题 | 答案 | 判定依据 |
|---|---|---|
| 需要热更新吗 | **需要**（唯一通道） | 改动全在 `public/`（CSS + JS），是纯前端 |
| 需要壳更新（APK）吗 | **不需要** | 没碰 `capacitor.config.json` / Capacitor 插件 / `AndroidManifest.xml` / `build.gradle` → 原生层零改动 |
| 需要补 `index.html` 的版本 meta 吗 | **不需要** | 构建期注入（`release.mjs` 在暂存副本上注入），仓库里恒为 `0.0.0` 占位值 |
| 需要 SQL 迁移吗 | 不需要（见第三节） | — |

**发布命令序列**（按铁律三）：

```bash
# 0. 前置：改动已合并 main、工作区干净（CD 只从 main 出包）
node scripts/query-latest-version.mjs      # 只读，查历史最高版本
#    当前线上最高 = 2.7.67  → 新版本号必须 > 2.7.67

# 1. 发布（本地直跑，或走 CI 的 CD · Web 热更新发布 + production 环境审批）
node scripts/release.mjs 2.7.68 --notes "待办列表：时光章节 + 纸与光材质"

# 2. 回读校验（写成功 ≠ 客户端拿得到）
node scripts/verify-release.mjs 2.7.68
```

**建议版本号**：单次合并发 **2.7.68**；若按第六节拆两次，则视觉 `2.7.68` / 章节 `2.7.69`。

**用户动作**：杀 App 重开两次（首次后台下载，二次生效）。

---

## 五、测试与验证

**必跑（铁律二）**：

```bash
node scripts/check-test-env.mjs                      # Web 通道隔离（测试库 ≠ 生产库）
node app-e2e/scripts/check-test-schema.mjs           # schema 契约
node app-e2e/scripts/check-rls.mjs                   # 表 RLS + 函数权限
node scripts/run-web-e2e.mjs                         # 4 个双账号用例（逐个归零 + 失败重试）
```

**最需要盯的用例**：`test_undo_complete.py`（完成 → 沉底/Toast → 撤销完成）。
章节头不改变 `.todo` 的节点序列（未完成仍在前），断言仍成立；但**这一步不能只靠推理，必须实跑**。

**人工目测清单（无法自动断言的部分）**：

1. **线上那 7 张隐藏款的渐变底是否还在**（本次头号风险，见第二节的级联陷阱）
2. 暖色「晒过的纸」在你的屏幕 / 护眼模式下是否偏脏
3. 20px 复选框的点按命中率（原来是 18px）
4. 完成态的「撤销完成」入口是否真的可发现（这是本次唯一的行为变化）
5. 章节头的 `.tl-chap` 在**第二次 render 之后**是否仍在正确位置（触发方式：完成一条 / 对方端改一条，让列表重排一次）

**硬限制 / 未验证项（交付时要如实列出）**：

- 真机色偏、材质感受**无法在 CI 里断言**，只能人工看
- 隐藏款是 15% 概率开奖，测试库里造 7 张不现实 → 目测时在**测试库**手工插一条 `rarity='legendary'` 的待办来验（⚠️ 铁律一：**只在测试库，生产库禁止**）
- 章节小计的文案在跨月/跨年边界（如 12 月 31 日 23:59 完成）需要**手工构造时间**验证归属

---

## 六、建议的拆分与顺序

**拆成两次独立发布**（符合仓库「一次只发一类改动」的纪律，各自可回滚）：

| 顺序 | 内容 | 通道 | 复杂度 | 版本号 | 为什么先发它 |
|---|---|---|---|---|---|
| 1️⃣ | **纸与光**（含「撤销完成」入口修复） | 热更新 | S | 2.7.68 | 纯 CSS、无逻辑风险、立刻可见；且它顺手修掉的那个缺陷与方案选择无关，早点发早点受益 |
| 2️⃣ | **时光章节** | 热更新 | M | 2.7.69 | 要动渲染逻辑，单独一次发布便于定位问题（若一起发，出问题时无法区分是 CSS 还是渲染） |

两次之间建议至少真机跑一天 —— 第 1 步就能回答「新材质是否成立」，避免在错的视觉上继续叠结构。

---

## 七、文档与审查（铁律四 / 铁律六）

- **`PRODUCT-SPEC.md`**：「功能规格 → 待办列表」章节要补「时间章节 + 章节小计」规则和排序说明；若新增了 semantic token，同步「数据模型 / 设计令牌」相关描述
- **面试素材**：收尾后把「CSS 级联把盲盒奖励静默吃掉」这个判断追加到 `~/Desktop/interview-prep` 的 `16-CI_CD实战-面试知识点沉淀.md`（该目录有自己的 AGENTS.md，动笔前先读，写完跑 `npm run build`）
- **`CODE-REVIEW.md` 六维度**：重点看 **正确性**（时区日界、fragment 重排）与 **可维护性**（章节头复用、稀有度权重提升），数据安全维度无写入操作

---

## 八、待你确认的 3 个取舍

1. **章节内怎么排**：现在完成项按 `created_at DESC` 排。改成按 `completed_at DESC` 更自然（刚完成的就排在「本月」章节第一条，反馈清晰），但会改变现有顺序感知 —— 改还是不改？
2. **顶栏 + 章节头的高度**：章节头若也做吸顶（提案里的新④），会和顶栏一起占约 100px。本组合（新①）默认**不吸顶**，确认一下是否接受「滚上去就看不到月份」。
3. **贴纸小计的归属口径**：`一起完成 N 件 · 解锁贴纸 M 张` 里的 M 按月归属，依据是 `stickers.unlocked_at`（开奖瞬间）。也就是说「9 月解锁 2 张」= 9 月开出来的，而不是「9 月完成的待办身上的贴纸」。这个口径对不对？
