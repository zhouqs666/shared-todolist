# 双人共享待办清单 — 产品需求文档（PRD）

> **文档版本**：v1.1
> **创建日期**：2026-07-19
> **状态**：✅ 评审通过 → 进入开发

---

## 1. 项目概述

### 1.1 一句话描述
一个为**两个人**设计的轻量级共享待办清单网页应用，两人看到同一份清单，互相添加、完成、删除任务，所有变更**实时同步**到对方屏幕。

### 1.2 解决什么问题
现有的待办 App 多为单人使用，或为团队协作而过于复杂（含项目、文件夹、权限等）。本项目面向**情侣 / 室友 / 搭子**这种「就两个人、共同生活中需要分工协作」的关系，提供极简、即时、零打扰的共享清单体验。

### 1.3 核心价值
- **极简**：只有「添加 / 完成 / 删除」，没有多余功能
- **实时**：一人操作，对方屏幕立刻更新，无需刷新
- **随时可用**：手机浏览器打开网址即用，无需下载安装

---

## 2. 目标与范围

### 2.1 V1 目标（本次开发）
- ✅ 两个固定账号登录后共享同一份清单
- ✅ 添加、完成、取消完成、删除任务
- ✅ 任何一人操作，对方**实时同步**（< 1 秒）
- ✅ 手机浏览器访问，移动端友好
- ✅ 部署到公网，任何地方可访问

### 2.2 不在 V1 范围内（后续迭代）
- ❌ 任务编辑、截止日期、提醒
- ❌ 任务分类、标签、优先级
- ❌ 任务指派、评论、附件
- ❌ 注册流程、找回密码
- ❌ 数据导出 / 多端历史同步
- ❌ 离线模式

---

## 3. 用户与使用场景

### 3.1 目标用户
两个固定用户（下文称 **用户A** 和 **用户B**），账号预先在服务端配置好。

### 3.2 典型场景
| 场景 | 操作 |
|------|------|
| 周末家务分工 | A 添加「倒垃圾」「洗衣服」，B 看到后认领完成 |
| 共同采购 | A 添加「买牛奶」「买鸡蛋」，逛超市时两人各自勾选 |
| 旅行准备 | 两人共同维护出行待办，谁做完谁勾选，对方实时看到进度 |

### 3.3 用户故事（User Stories）
1. 作为用户A，我想**添加一条待办**，让用户B 立即看到。
2. 作为用户B，我想**勾选完成**一条待办，让用户A 看到这条已被完成。
3. 作为任意用户，我想**取消完成**或**删除**误操作的任务。
4. 作为任意用户，我想看到每条待办是**谁添加的**、**谁完成的**。
5. 作为任意用户，我想在**手机浏览器**里顺畅使用。
6. 作为任意用户，我想**刷新页面或重新打开**后，数据还在。

---

## 4. 功能需求

### 4.1 V1 功能清单

| 编号 | 功能 | 优先级 | 描述 |
|------|------|--------|------|
| F-01 | 登录 | P0 | 用户名 + 密码登录，预置双账号 |
| F-02 | 添加待办 | P0 | 输入文本，回车 / 点击按钮添加 |
| F-03 | 完成待办 | P0 | 点击 checkbox 标记完成，划线显示 |
| F-04 | 取消完成 | P0 | 再次点击 checkbox 取消完成状态 |
| F-05 | 删除待办 | P0 | 删除按钮，二次确认（移动端友好） |
| F-06 | 实时同步 | P0 | 任何变更通过 WebSocket 推送给对方 |
| F-07 | 创建者标识 | P1 | 每条待办显示「谁添加的 / 谁完成的」 |
| F-08 | 退出登录 | P2 | 清除会话 |
| F-09 | 空状态引导 | P2 | 没有待办时显示友好提示 |

### 4.2 详细功能说明

#### F-01 登录
- **入口**：未登录访问任何页面都跳转到 `/login`
- **输入**：用户名、密码
- **校验**：服务端比对数据库 `users` 表中的预置账号
- **账号初始化**：服务首次启动时若 `users` 表为空，从环境变量（用户名 + bcrypt hash）自动写入两条预置账号（Q4 决策）
- **会话**：登录成功后设置 Cookie / Token（有效期 30 天），后续请求携带
- **失败提示**：「用户名或密码错误」，不区分具体原因（安全考虑）

#### F-02 添加待办
- **输入框**：固定在页面顶部，placeholder「添加一条待办...」
- **操作**：输入文本后按回车 / 点击「+」按钮
- **校验**：去掉首尾空格后非空，最长 200 字符
- **默认值**：新建任务 = 未完成；创建者 = 当前登录用户；创建时间 = 服务端时间
- **完成后**：清空输入框，新任务**出现在列表顶部**

#### F-03 / F-04 完成与取消
- **操作**：点击任务前的 checkbox
- **视觉效果**：完成后文本加删除线 + 灰色；取消后恢复
- **排序**：完成的任务**下沉**到底部（Q2 决策），同区间内仍按创建时间倒序
- **完成者**：记录「谁完成的」+ 完成时间

#### F-05 删除
- **操作**：每条任务右侧有删除按钮（垃圾桶图标）
- **确认**：点击后弹出二次确认（移动端用 `confirm()` 或自定义弹窗）
- **不可恢复**：删除后直接物理删除（V1 不做回收站）

#### F-06 实时同步（核心）
- **技术**：Socket.IO（基于 WebSocket，自动降级到轮询）
- **事件**：服务端在任意数据变更后，向**所有在线连接**广播
- **场景**：
  - A 添加任务 → 服务端广播 `todo:created` → B 前端追加
  - A 完成任务 → 服务端广播 `todo:updated` → B 前端更新样式
  - A 删除任务 → 服务端广播 `todo:deleted` → B 前端移除
- **降级**：断线自动重连；重连后拉取最新全量数据

#### F-07 创建者 / 完成者标识
- 任务下方小字显示：`A 添加 · 3分钟前`
- 完成后追加：`· B 完成`（Q3 决策：同时显示创建者与完成者）

---

## 5. 非功能需求

### 5.1 性能
- 实时同步延迟 **< 1 秒**（同城网络）
- 首屏加载 **< 2 秒**
- 单次操作服务端响应 **< 300ms**

### 5.2 兼容性
- **移动端优先**：iOS Safari、Android Chrome 最新版
- **桌面端**：Chrome / Safari / Firefox / Edge 最新版
- **响应式**：宽度自适应 320px ~ 1920px

### 5.3 可用性
- 服务可用性 95%+（免费 PaaS 不强求更高）
- 数据持久化：服务重启后数据保留（见 §10 部署注意事项）

### 5.4 安全
- 密码 **bcrypt 哈希存储**，不明文
- 登录态 Cookie 设置 `HttpOnly` + `Secure`（HTTPS 环境）+ `SameSite=Lax`
- 所有数据接口校验登录态，未登录返回 401
- 输入做 XSS 过滤（转义 HTML 特殊字符）

### 5.5 可维护性
- 代码结构清晰，前后端分离（即便在一个仓库）
- 配置（账号、端口、密钥）走环境变量
- 关键逻辑加单元测试（V1 可选，建议核心同步逻辑覆盖）

---

## 6. 技术架构

### 6.1 技术选型

| 层 | 技术 | 说明 |
|----|------|------|
| 前端 | 原生 HTML5 + CSS3 + JavaScript (ES6+) | 不引框架，零构建，简单清晰 |
| 实时通信 | Socket.IO Client | 自动重连、降级、房间 |
| 后端 | Node.js + Express | 轻量、生态成熟 |
| WebSocket | Socket.IO Server | 与客户端配套 |
| 数据存储 | Supabase PostgreSQL（免费层） | Q1 决策；外部托管，规避免费 PaaS 临时文件系统问题 |
| 会话 | Cookie + express-session 或 JWT | V1 选 Cookie 更简单 |
| 密码 | bcrypt | 行业标准 |

### 6.2 系统架构图（文字版）

```
┌─────────────┐         ┌──────────────────────────────┐
│  用户A 手机  │         │        Node.js 服务          │
│   浏览器    │◄───────►│  ┌────────────────────┐      │
│  (前端页面) │ HTTPS   │  │  Express (REST)    │      │
└─────────────│ +WS     │  ├────────────────────┤      │
              │         │  │  Socket.IO (实时)   │      │
┌─────────────┐         │  ├────────────────────┤      │
│  用户B 手机  │◄───────►│  │  数据访问层        │      │
│   浏览器    │         │  └─────────┬──────────┘      │
└─────────────┘         └────────────┼─────────────────┘
                                     │
                          ┌──────────▼──────────┐
                          │   SQLite / JSON 文件 │
                          │   (持久化存储)        │
                          └─────────────────────┘
```

### 6.3 目录结构（建议）

```
toDoList/
├── README.md
├── PRD.md                 # 本文档
├── package.json
├── .env.example           # 环境变量模板
├── .gitignore
├── render.yaml            # Render 部署配置（可选）
│
├── src/                   # 后端
│   ├── index.js           # 入口：启动 Express + Socket.IO
│   ├── server.js          # HTTP/Socket 服务挂载
│   ├── routes/
│   │   ├── auth.js        # 登录 / 登出
│   │   └── todos.js       # 待办 CRUD（REST）
│   ├── sockets/
│   │   └── index.js       # Socket.IO 连接与事件处理
│   ├── store/
│   │   ├── todoStore.js   # 数据访问层入口（按 DB_TYPE 切换实现）
│   │   ├── jsonStore.js   # 本地开发：JSON 文件实现
│   │   └── supabaseStore.js  # 生产：Supabase PostgreSQL 实现
│   ├── auth/
│   │   └── middleware.js  # 登录态校验中间件
│   ├── config/
│   │   └── users.js       # 从 env 读取双账号配置 + 首启动初始化
│   └── utils/
│       └── validation.js  # 输入校验
│
├── public/                # 前端静态资源
│   ├── index.html         # 主页（清单页）
│   ├── login.html         # 登录页
│   ├── css/
│   │   └── style.css      # 样式（移动优先）
│   └── js/
│       ├── app.js         # 主逻辑
│       ├── socket.js      # Socket 客户端封装
│       └── api.js         # REST 调用封装
│
└── data/                  # 运行时数据（仅本地开发）
    └── todos.json         # JSON 存储（git 忽略；仅 DB_TYPE=json 时使用）
```

---

## 7. 数据模型设计

### 7.1 用户（User）

> V1 用户**预置**在配置中，无注册流程。下表描述字段。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 用户唯一标识，如 `"u_a"` |
| username | string | 登录用户名，如 `"alice"` |
| displayName | string | 显示名，如 `"Alice"` |
| passwordHash | string | bcrypt 哈希 |

### 7.2 待办（Todo）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | UUID，主键 |
| text | string | 任务内容，≤ 200 字符 |
| completed | boolean | 是否完成，默认 `false` |
| createdBy | string | 创建者 userId |
| createdAt | ISO 8601 | 创建时间（服务端） |
| completedBy | string \| null | 完成者 userId，未完成为 `null` |
| completedAt | ISO 8601 \| null | 完成时间 |

> **不带** `updatedBy/updatedAt`：删除即物理删除，完成/取消复用 `completedBy/At`。

### 7.3 SQL 示例（如选 SQLite）

```sql
CREATE TABLE IF NOT EXISTS users (
  id            TEXT PRIMARY KEY,
  username      TEXT UNIQUE NOT NULL,
  display_name  TEXT NOT NULL,
  password_hash TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS todos (
  id            TEXT PRIMARY KEY,
  text          TEXT NOT NULL,
  completed     INTEGER NOT NULL DEFAULT 0,
  created_by    TEXT NOT NULL,
  created_at    TEXT NOT NULL,
  completed_by  TEXT,
  completed_at  TEXT,
  FOREIGN KEY (created_by)   REFERENCES users(id),
  FOREIGN KEY (completed_by) REFERENCES users(id)
);

CREATE INDEX idx_todos_created_at ON todos(created_at);
```

---

## 8. 接口设计

### 8.1 REST API

| 方法 | 路径 | 说明 | 鉴权 | 入参 | 出参 |
|------|------|------|------|------|------|
| POST | `/api/login` | 登录 | ❌ | `{username, password}` | `{ok: true}` + Set-Cookie |
| POST | `/api/logout` | 登出 | ✅ | — | `{ok: true}` |
| GET | `/api/me` | 当前用户 | ✅ | — | `{id, displayName}` |
| GET | `/api/todos` | 全部待办 | ✅ | — | `Todo[]` |
| POST | `/api/todos` | 新建 | ✅ | `{text}` | `Todo` |
| PATCH | `/api/todos/:id` | 更新（完成/取消） | ✅ | `{completed}` | `Todo` |
| DELETE | `/api/todos/:id` | 删除 | ✅ | — | `{ok: true}` |

**错误响应统一格式**：
```json
{ "ok": false, "error": "UNAUTHORIZED" | "NOT_FOUND" | "INVALID_INPUT" | "INTERNAL" }
```

### 8.2 Socket.IO 事件

**客户端 → 服务端**：
| 事件 | 触发场景 | 备注 |
|------|---------|------|
| `connection` | 页面加载 | 连接成功后服务端推送 `todo:sync`（当前全量） |

> V1 的增删改走 REST（请求-响应清晰、便于返回错误），Socket 仅用于**推送通知**。这是更稳健的设计，避免实时事件的幂等性问题。

**服务端 → 客户端**：
| 事件 | 数据 | 触发时机 |
|------|------|---------|
| `todo:sync` | `Todo[]` | 客户端连上时下发全量 |
| `todo:created` | `Todo` | 任意用户新建后广播 |
| `todo:updated` | `Todo` | 任意用户完成/取消后广播 |
| `todo:deleted` | `{id}` | 任意用户删除后广播 |

广播范围：当前命名空间下所有连接（双人共享一份清单）。

---

## 9. 页面与交互设计

### 9.1 页面清单
1. `/login` — 登录页
2. `/` — 主页（清单页，登录后访问）

### 9.2 主页布局（移动优先）

```
┌────────────────────────────┐
│  📋 我们的清单      [退出] │  ← 顶栏（显示当前用户名）
├────────────────────────────┤
│ [添加一条待办...        +] │  ← 输入区（固定顶部）
├────────────────────────────┤
│ ☐ 取快递  · A · 3分钟前   │
│ ☐ 买牛奶  · B · 10分钟前  │
│ ☑ 倒垃圾  · A添加 · B完成 │  ← 已完成（划线、灰）
│         [🗑]               │
│                            │
│   （没有更多了）           │
└────────────────────────────┘
```

### 9.3 交互细节
- 输入框聚焦时键盘弹出，页面不抖动
- checkbox 点击有微动画
- 删除按钮点击 → 二次确认
- 网络断开 → 顶部出现「已离线」红条；恢复后消失
- 加载中 → 任务区显示骨架屏 / loading

### 9.4 视觉风格（建议）
- 主色：温和的蓝绿（如 `#3B82F6` 或 `#10B981`）
- 背景：浅灰 `#F9FAFB`
- 圆角、留白充足
- 字号：移动端正文 16px 起
- 字体：系统默认（`-apple-system, "PingFang SC", ...`）

> UI 细节可在开发阶段进一步评审，本 PRD 不锁定像素级设计。

---

## 10. 部署与运维

### 10.1 部署目标
- **平台**：Render（免费层 Web Service）
- **访问**：`https://<your-app>.onrender.com`
- **HTTPS**：平台自动签发证书（手机访问必须）

### 10.2 数据持久化方案（Q1 已决策）

Render 免费层 Web Service 的**文件系统是临时的**（ephemeral），每次重启 / 重新部署，本地文件都会丢失。

**最终方案**：
- **本地开发**：JSON 文件存储（零依赖、快速跑通）
- **生产部署**：**Supabase 免费层 PostgreSQL**（已确认引入）
- **抽象**：数据访问层（`src/store/todoStore.js`）封装两种实现，通过 `DB_TYPE` 切换，业务代码无感知

**Supabase 配置要点**：
- 注册免费账户，创建项目（免费层 500MB 存储，2 人场景绰绰有余）
- 在 SQL Editor 执行 §7.3 的建表语句
- 取 Connection String 和 anon key 配置到环境变量
- 启用 Row Level Security 后可加最简单的 policy（V1 可选，鉴权已在应用层做）

### 10.3 环境变量（`.env`）

```bash
# 服务
PORT=3000
NODE_ENV=production
SESSION_SECRET=<随机长字符串>

# 双账号（V1 预置，建议生产用 bcrypt 预生成 hash 填入）
USER_A_USERNAME=alice
USER_A_DISPLAY_NAME=Alice
USER_A_PASSWORD_HASH=$2b$10$xxxxx
USER_B_USERNAME=bob
USER_B_DISPLAY_NAME=Bob
USER_B_PASSWORD_HASH=$2b$10$xxxxx

# 数据存储（按所选方案）
DB_TYPE=supabase            # json | supabase（本地开发用 json）
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_KEY=xxx
SUPABASE_DB_URL=postgresql://postgres:xxx@db.xxx.supabase.co:5432/postgres
```

### 10.4 免费层限制提醒
- Render 免费服务 **15 分钟无请求会休眠**，首次唤醒约 30~50 秒
- 休眠期间 Socket 连接会断开，前端需做**自动重连**（Socket.IO 默认支持）
- 流量 / 在线时长有上限（750h/月），双人场景完全够用

---

## 11. 开发计划与里程碑

### 11.1 阶段拆分

| 阶段 | 内容 | 预估工时 |
|------|------|---------|
| **M1：项目脚手架** | 初始化项目、目录结构、Express 起服务、静态页面能访问 | 0.5 天 |
| **M2：登录与会话** | 预置账号、登录页、登录态中间件、退出 | 0.5 天 |
| **M3：待办 CRUD（REST）** | 数据存储层 + REST 接口 + 单人本地能添加/完成/删除 | 1 天 |
| **M4：实时同步（Socket）** | Socket.IO 接入、广播事件、客户端处理 | 0.5 天 |
| **M5：前端打磨** | 移动端样式、交互细节、空状态、断线提示 | 1 天 |
| **M6：部署上线** | Render 部署、Supabase 接入、环境变量、域名验证 | 0.5 天 |
| **M7：联调与测试** | 双端真实场景测试、Bug 修复 | 0.5 天 |

**合计：约 4.5 人天**

### 11.2 验收标准（DoD）
- [ ] 两个真实浏览器（或一台手机 + 一台电脑）登录不同账号
- [ ] 一方添加任务，另一方 1 秒内出现
- [ ] 一方完成 / 取消 / 删除，另一方 1 秒内同步
- [ ] 刷新页面、重开浏览器，数据保留
- [ ] 手机 Safari / Chrome 打开网址正常使用
- [ ] 未登录访问主页跳转登录页
- [ ] 部署后公网可访问（HTTPS）

---

## 12. 风险与待定事项

### 12.1 待决策（请评审确认）

> ✅ 评审已完成，全部决策如下，无遗留待定项。

| # | 事项 | 决策 |
|---|------|------|
| Q1 | 数据存储方案 | ✅ **Supabase 免费 PostgreSQL** |
| Q2 | 任务排序 | ✅ **新任务置顶，已完成的下沉**，同区间内按创建时间倒序 |
| Q3 | 是否显示「谁完成」 | ✅ **显示**，同时显示创建者与完成者 |
| Q4 | 账号预置方式 | ✅ **环境变量配置 + 首次启动自动写入数据库** |
| Q5 | 休眠唤醒延迟 | ✅ **接受** 30~50s，前端做 loading 提示，暂不升级 |

### 12.2 已知风险
- **R1 数据丢失**：免费 Render 重启丢本地文件 → 用外部数据库规避
- **R2 冷启动慢**：休眠唤醒需等待 → 前端 loading 提示 / 可选升级
- **R3 安全性**：自实现登录需谨慎 → 严格按 §5.4 安全清单实施
- **R4 双账号并发**：两人同时操作同一任务 → 服务端按到达顺序串行处理

---

## 13. 附录

### 13.1 术语表
| 术语 | 含义 |
|------|------|
| PRD | 产品需求文档（本文档） |
| DoD | Definition of Done，完成标准 |
| PaaS | 平台即服务（如 Render） |
| WebSocket | 全双工长连接协议，用于实时通信 |
| Socket.IO | 基于 WebSocket 的实时通信库 |

### 13.2 参考链接
- Express：https://expressjs.com/
- Socket.IO：https://socket.io/
- Render：https://render.com/
- Supabase：https://supabase.com/

---

**评审通过后，开发将依据本 PRD 的 §11 计划分阶段进行。任何变更需更新本文档版本号。**
