# Web 通道 E2E（scripts/test_*.py）的 Python 依赖
#
# 为什么需要这个文件：这 4 个用例原本只在本机跑，依赖「本机碰巧装过 playwright」。
# 进 CI（.github/workflows/e2e-web-full.yml）后必须可复现：版本写死，
# 浏览器二进制用同一个版本号下载（playwright install chromium）。
#
# 安装：
#   python3 -m pip install -r scripts/requirements-e2e.txt
#   python3 -m playwright install --with-deps chromium
#
# 升级步骤（不要只改版本号）：改这里 → 本地重跑 node scripts/run-web-e2e.mjs 全绿 → 再改 CI。
# 版本漂移会让「本地绿 / CI 红」重新变成玄学，正好是本批次要消灭的东西。

playwright==1.60.0
