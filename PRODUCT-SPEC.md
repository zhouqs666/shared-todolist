# 有爱 · 产品规格文档（Product Specification）

> **文档版本**：v3.3
> **撰写日期**：2026-09-08
> **对应产品版本**：前端 Bundle `2.7.61` / 原生壳 `2.1.28`（versionCode 32）
> **文档性质**：**现状描述型规格文档（As-is Spec）**——记录产品"现在是什么样"，而非"要做什么"
> **与历史文档的关系**：旧的 V1 立项需求文档（`PRD.md`）和 `README.md` 已于 2026-09-07 删除——前者描述了已废弃的 Express+Socket.IO 自建服务架构，后者内容与本文档大量重复且多处与当前代码不符。本文档是项目唯一的现行规格。

---

## 0. 写在前面：为什么需要这份文档

这款 App 从"一个双人共享待办清单"开始，经过长期高频迭代，已经长成了一个**带情感化设计的双人协作产品**：待办只是骨架，真正让它被每天打开的是隐藏款盲盒、贴纸图鉴、阅后即焚的心里话、纪念日倒计时这些"没什么用但很想点开"的东西。

但代码里积累了大量只有写代码的人知道的隐性知识：哪个字段接了 UI、哪个字段是半成品、哪个注释说的是一回事而代码做的是另一回事。这份文档的目的就是把这些**从代码里挖出来、写清楚**，让任何一个接手的人（包括三个月后的你）能在一小时内看懂这个 App 的全貌。

> ⚠️ **本文档的诚实声明**：所有功能描述均基于 2026-09-07 的代码事实核对。凡是代码里存在但未接入 UI、或注释与实现不一致的地方，都在 **§9 已知限制与技术债** 中明确标出，不做粉饰。

---

## 1. 产品概述

### 1.1 一句话描述

**「有爱」是一个只属于两个人的待办清单——你加一条，ta 那边立刻亮起来；做完的瞬间会有彩带、音效和一句夸奖；偶尔还会开出一张隐藏款贴纸。**

### 1.2 产品定位

| 维度 | 定位 |
|------|------|
| 产品形态 | Capacitor 混合 App（Android APK 为主）+ PWA（可装主屏） |
| 核心场景 | 情侣 / 伴侣之间的共同生活协作（家务、采购、出行准备） |
| 用户规模 | **2 人**（固定账号，无注册入口，不做多人） |
| 设计取向 | 情感化 > 效率化；仪式感 > 信息密度 |
| 技术取向 | 无自建服务器，前端直连 Supabase（BaaS） |
| 成本模型 | 全免费（Supabase 免费层 + 自建分发） |

### 1.3 核心价值（按重要性排序）

1. **共享感**——一份清单两个人看，任何操作 < 1 秒同步到对方屏幕，是"我们在一起做事"的实感。
2. **即时正反馈**——完成一件事有彩带、音效、震动、夸奖文案，把"打勾"变成一个值得期待的动作。
3. **轻量无负担**——没有项目、标签、优先级、子任务，打开就能用，三秒内完成一次添加。
4. **小惊喜**——15% 概率的隐藏款待办、12 张收集图鉴、不知道什么时候会飘来的心里话，制造打开理由。
5. **关系锚点**——顶栏常驻"在一起 N 天"、纪念日倒计时，让工具承载关系记忆。

### 1.4 产品现状快照

| 项目 | 当前状态 |
|------|---------|
| 前端 Bundle 版本 | `2.7.61` |
| 原生壳版本 | `2.1.28`（versionCode 32） |
| 代码规模 | 前端 JS 约 25 个模块，主文件 `app.js` 约 60KB |
| Git 提交 | 29 次（实际迭代次数远超，早期未纳入版本管理） |
| 平台 | Android APK（主）+ 浏览器 PWA（备） |
| 后端 | Supabase 免费层（PostgreSQL + Auth + Realtime + Storage） |

---

## 2. 目标用户与使用场景

### 2.1 用户画像

产品为**特定两个人**定制，而非通用市场。账号固定为两个：

| 显示名 | 登录标识 | 说明 |
|--------|---------|------|
| 小宝宝 | `xiaobaobao@todo.local`（支持中文名直接登录） | 用户 A |
| 大宝贝 | `dabaobei@todo.local`（支持中文名直接登录） | 用户 B |

> 密码在账号初始化时预置，本文档不记录明文凭据。

**画像特征**：
- 共同生活的伴侣，有明确的分工协作需求（家务、采购、出行）
- 对工具的效率要求不高，但对"对方在不在"、"ta 有没有看到"很敏感
- 会被小惊喜驱动，愿意为了集齐图鉴多完成几件事
- 不需要团队协作功能（这恰恰是本产品刻意不做的地方）

### 2.2 典型场景

| 场景 | 用户行为 | 产品承接 |
|------|---------|---------|
| **周末家务分工** | A 添加"倒垃圾""洗衣服"，B 看到后认领完成 | 实时同步 + 完成者标识 |
| **共同采购** | A 加"买牛奶"，B 在超市勾选 | 实时同步 + 移动端单手操作 |
| **说干就干的提醒** | A 加了条待办，B 手机后台收到系统通知 | 本地通知（仅 App 在后台时） |
| **完成后的交代** | B 完成"打蚊子"后加备注"书房那只已解决" | 完成备注功能 |
| **当面说不出口的话** | 借"心里话"发一句悄悄话，对方看完即焚 | 阅后即焚的 daily_notes |
| **偶尔的惊喜** | 加待办时开出"传说"隐藏款，撒金色彩带 | 隐藏款盲盒 + 贴纸图鉴 |
| **翻旧账式浪漫** | 点顶栏桃心，看"在一起 2xxx 天"和生日倒计时 | 纪念日面板 |

### 2.3 用户故事

1. 作为**任一方**，我希望加一条待办后对方**立刻**看到，以便不用再口头通知一遍。
2. 作为**任一方**，我希望看到每条待办是**谁加的、谁做完的**，以便知道事情的来龙去脉。
3. 作为**任一方**，我希望完成时有**庆祝反馈**，以便做家务这种琐事也有成就感。
4. 作为**任一方**，我希望能给完成的待办**加一句备注**，以便交代完成情况（"已买，放冰箱了"）。
5. 作为**任一方**，我希望误删的待办**不会真的消失**，以便关键时刻能找回。
6. 作为**任一方**，我希望给待办**配张图**，以便说清楚"买这个样子的"。
7. 作为**任一方**，我希望有一处能**写下当面说不出口的话**，且看完不留痕迹。
8. 作为**任一方**，我希望知道**ta 今天来过没有**，以便感觉彼此的连接。
9. 作为**任一方**，我希望打开 App 时**自动拿到最新版本**，不用手动去下载安装包。
10. 作为**任一方**，我希望在**没网的时候**不至于白屏一片。

---

## 3. 产品差异化

### 3.1 与普通待办工具的区别

| 对比项 | 有爱 | 滴答清单 / Todoist / Microsoft To Do |
|--------|------|--------------------------------------|
| 用户数 | 固定 2 人 | 个人为主，团队需付费 |
| 协作模型 | 全共享，无权限概念 | 需要清单共享、指派、评论 |
| 功能复杂度 | 极简（添加/完成/删除） | 项目、标签、优先级、子任务、过滤器 |
| 情感化设计 | 核心能力（彩带/音效/盲盒/心里话） | 基本没有 |
| 数据归属 | 自己的 Supabase，可完全掌控 | 第三方云服务 |
| 学习成本 | 零 | 中到高 |

**一句话差异**：通用待办工具解决"把事记下来"，「有爱」解决"让我们两个一起把事做完，并且过程中感觉不错"。

### 3.2 与"情侣 App"类产品的区别

市面上的情侣 App（如 Between、小恩爱、微爱）通常提供情侣空间、相册、恋爱打卡、经期记录等。**「有爱」刻意不做这些**，原因：

- 这些功能的使用频率远低于共同事务，容易变成"下载三天后再也不打开"
- 待办是**唯一一个两个人每天都有真实协作需求**的场景，天然高频
- 情感元素以**点缀**方式附着在高频动作上（完成时有彩带、加待办可能开出隐藏款），而不是另起炉灶做一个"情侣空间"

> **关键设计判断**：情感化不是靠堆功能，而是把正反馈嫁接到用户本来就要做的动作上。这也是为什么本产品没有"签到""恋爱日记"这类需要额外意志力维持的功能。

---

## 4. 信息架构

### 4.1 页面结构

```
有爱 App
├── 登录页 (login.html)
│   └── 用户名 + 密码 → Supabase Auth
│
└── 主页 (index.html)                      ← 唯一主界面，单页结构
    ├── 顶栏
    │   ├── 桃心按钮          → 展开【纪念日面板】
    │   │   └── 纪念日面板
    │   │       ├── 在一起 N 天（正计时）
    │   │       ├── 结婚纪念日倒计时
    │   │       ├── 小宝宝生日倒计时
    │   │       ├── 大宝贝生日倒计时
    │   │       └── 「留一句心里话」入口
    │   ├── 留言信封（有未读时显现 + 呼吸光晕）
    │   ├── 图鉴入口（有新解锁时红点）
    │   ├── 壳更新入口（有新 APK 时显现）
    │   └── 当前用户名
    ├── 离线提示条（网络断开时顶部滑出）
    ├── 待办列表
    │   └── 待办项
    │       ├── 复选框（完成/取消）
    │       ├── 文本 + 元信息（谁加的/谁做的/多久前）
    │       ├── 缩略图（可多张）
    │       ├── 完成备注（「」包裹）
    │       ├── 稀有度标识（隐藏款）
    │       └── 爱心反应（仅已完成的待办）
    ├── 悬浮添加按钮 (FAB)
    │   └── 添加面板（底部上滑）
    │       ├── 文本输入（≤200 字）
    │       └── 配图按钮
    └── 浮层
        ├── 心里话写信页（全屏，阅后即焚）
        ├── 心里话阅读页（全屏，翻页即焚 / 长按珍藏）
        ├── 收集图鉴弹层（12 格 + 进度 + 分档图例）
        ├── 图片全屏预览 (lightbox，支持双指缩放)
        └── 长按菜单（编辑 / 配图 / 备注 / 反应 / 删除）
```

### 4.2 交互手势约定

| 手势 | 作用 |
|------|------|
| 点击复选框 | 完成 / 取消完成 |
| 长按待办（350ms）/ 右键 | 唤出操作菜单 |
| 点击顶栏桃心 | 展开纪念日面板 |
| 点击 FAB | 展开添加面板 |
| 图片全屏：横向滑动 | 切换图片 |
| 图片全屏：双指捏合 | 缩放（最大 4×） |
| 图片全屏：双击 | 放大 / 还原 |
| 图片全屏：单击 | 关闭 |
| 心里话：单击「阅」 | 翻到下一条（当前这条焚毁） |
| 心里话：长按 600ms「珍藏」 | 文字化作爱心飞向桃心（当前这条焚毁） |

> 复选框有 **350ms 长按判定**：按下超过 350ms 不触发完成，防止误触。

---

## 5. 功能规格

> 状态图例：✅ 已上线 ｜ ⚠️ 部分实现／有缺口 ｜ 🔒 已建能力但未接入 UI

### 5.1 账号与登录 `F-01` ✅

| 项 | 规格 |
|----|------|
| 账号数量 | 固定 2 个，无公开注册入口 |
| 创建方式 | 用 service_role 调 `auth.admin.createUser` 创建（一次性运维脚本，使命完成后已移除） |
| 登录方式 | 邮箱或中文昵称 + 密码，走 Supabase Auth `signInWithPassword` |
| 显示名 | 「小宝宝」「大宝贝」（初始化时设置） |
| 会话 | Supabase 持久化 session，登录一次后长期保持 |
| 未登录 | 自动跳转登录页 |
| 头像 | 支持账号头像显示（`avatars.js`） |

**设计说明**：不提供注册是有意的安全设计——正因为全世界只有这两个账号能登录，"所有已登录用户可读写全部数据"的宽松 RLS 策略才是安全的。

### 5.2 待办核心流程 `F-02` ✅

| 操作 | 触发方式 | 规则 |
|------|---------|------|
| **添加** | FAB → 输入 → 回车/点击 | 文本 ≤ 200 字，去空格后非空；乐观更新，新待办置顶 |
| **完成** | 点击复选框 | 记录 `completedBy`/`completedAt`；乐观更新，失败回滚 |
| **取消完成** | 再次点击复选框 | 清空完成者与时间 |
| **编辑** | 长按 → 菜单 → 编辑 | ≤ 200 字，不允许为空 |
| **删除** | 长按 → 菜单 → 删除 | **软删除**（标记 `deleted_at`），Toast 提示"已移到回收站" |
| **配图** | 添加时预选，或长按 → 配图 | 支持多选追加，见 F-03 |
| **加备注** | 长按 → 加备注 | ≤ 100 字，见 F-04 |
| **置顶** | 长按 → 置顶/取消置顶 | 置顶项显示在列表最上方 |

**排序规则**：置顶项在最上方 → 未完成的在上 → 已完成的沉底；同状态内按创建时间倒序（新的在前）。

**并发处理**：`latestIntent` + `inFlight` 竞态守卫，避免快速连点导致状态错乱；Realtime 回推的数据做幂等去重。

> ✅ 删除已支持撤销：删除后弹出「撤销」Toast（5 秒内可撤回），误删可在回收站恢复。

### 5.3 图片附件 `F-03` ✅

| 项 | 规格 |
|----|------|
| 数量 | 单条待办支持**多图**（`image_paths` JSONB 数组） |
| 上传压缩 | 短边 ≤ 1280 直传原图；> 1280 等比缩至短边 1280；PNG 无损，其余转 JPEG（质量 0.92）；webp/gif 原样上传 |
| 存储 | Supabase Storage 公开 bucket `todo-attachments` |
| 预览 | 缩略图列表 → 点击全屏 lightbox |
| 全屏能力 | 横滑切图、双指缩放（≤4×）、双击放大、放大态拖拽、长图纵向拖动、单击关闭 |
| 删除 | **两段式确认**（首次点击提示"再点一次确认删除"，3 秒内二次点击生效） |

> ⚠️ **缺口**：删除图片只解绑数据库字段，**不删除 Storage 上的文件**，会持续占用存储（见 §9 T-06）。

### 5.4 完成备注 `F-04` ✅

完成后可以给这条待办留一句话（如"书房那只蚊子已打死"），独立于完成状态存在，可修改、可清空，≤100 字，在待办元信息行以「」包裹显示。

**产品价值**：把"完成"从一个二进制状态变成一次**可以交代的收尾**，是协作闭环的最后一环。

### 5.5 软删除与回收站 `F-05` ✅

删除仅标记 `deleted_at`，**不物理删除**，数据库保留数据。这是 2026-07-31 一次误删事故后确立的安全机制。

| 能力 | 说明 |
|------|------|
| 删除 | 软删除 + 撤销 Toast（5 秒内可撤回） |
| 回收站 | 头像长按 → 账号菜单 → 回收站，列出已删除待办，支持**恢复**与**彻底删除**（两段式确认） |
| 物理删除 | 彻底删除前自动清理该待办关联的图片（Storage bucket），避免存储泄漏 |
| 删除不会被"复活" | 本地对已删除待办打**墓碑**：迟到的 Realtime 回声（WebSocket 重连期间的事件会补推）与"全量重拉列表"都不能再把它装回主列表（2026-09-16 修）。用户在回收站/撤销里明确恢复时才撤墓碑 |

### 5.6 实时同步 `F-06` ✅

| 项 | 规格 |
|----|------|
| 技术 | Supabase Realtime（WebSocket） |
| 频道 | `todos-changes` |
| 订阅表与事件 | `todos`（INSERT/UPDATE/DELETE）、`daily_notes`（INSERT/UPDATE/DELETE）、`reactions`（INSERT/DELETE）、`stickers`（INSERT） |
| 同步延迟 | < 1 秒 |
| 冲突处理 | 乐观更新 + 服务端按到达顺序串行；回推数据幂等去重 |
| 断线处理 | 顶部离线提示条（随机诗意文案），恢复后自动重连并拉取全量 |

**关键实现**：所有写操作走 REST（`supabase-js`），Realtime **只用于推送通知**——避免实时事件的幂等性问题，是更稳健的架构选择。

### 5.7 在线状态与"今天来过" `F-07` ✅

两套机制互补：

| 机制 | 实现 | 特点 |
|------|------|------|
| **实时在线** | Realtime Presence 频道 `online-presence`，15 秒心跳 | 临时态，断开即失 |
| **今天来过** | `profiles.last_seen_at` 持久化字段 | 跨会话，判断"今天是否来过" |

**附加设计——爱心光晕**：`profiles.login_count_for_partner` 记录"对方没看到时我打开了 App 几次"。对方打开时顶栏爱心**闪 N 下光晕**然后清零（RPC `increment_login_count` / `consume_login_count`，SECURITY DEFINER 绕过 RLS 保证原子性）。

> 这是一个很妙的设计：它把"我来过"变成了一个不需要文字的、可被感知的信号。

### 5.8 隐藏款盲盒 `F-08` ✅

| 项 | 规格 |
|----|------|
| 触发时机 | **添加待办时** |
| 触发概率 | 15% |
| 稀有度分档 | 命中后按 60 / 30 / 10 分配 `rare`（稀有）/ `epic`（史诗）/ `legendary`（传说） |
| 已见标记 | `rarity_seen` 控制对方端首次见到时是否播放惊喜提示。**创建隐藏款时写 `false`**（2026-09-16 修：此前恒为 `true`，导致对方端的揭晓提示从未触发过）；对方端播完提示回标 `true`；若对方当时不在线，本端下次冷启动会补播一次（只播最新一条） |
| 庆祝表现 | rare：专属文案 Toast；epic/legendary：撒花 + 震动 + `burstCardRing` 卡片光环（legendary 三圈 + 金光） |
| 提示合并 | 开奖文案与"解锁了哪张贴纸"**合成一条**提示（2026-09-16 修）。旧实现是两条独立提示，共用一个 toast 节点、后一条当场顶掉前一条，于是"图鉴已集齐""图片上传失败"这类信息用户看不到 |
| 提示串行 | 全局 Toast 改为**串行展示**（上一条还在显示时，新提示排队等它消失，不再互相覆盖）。错误类提示用 `urgent` 抢占 |

> ✅ **已确认**（2026-09-07 核实）：贴纸解锁时机是**添加待办开出隐藏款时**（`onRollRarity`），完成隐藏款待办时只播庆祝动画、不重复解锁。`migration-blindbox-stickers.sql` 的注释已同步修正。
>
> ✅ **2026-09-16 修正**：完成隐藏款待办时，完成提示（含"图鉴 n/12"与「撤销」按钮）此前会被开奖提示当场清掉，
> 用户看到的是语义错误的"开出X款！"且没有撤销入口；现在 `celebrateRarity` 只放特效、不再自己弹提示。

### 5.9 收集图鉴 `F-09` ✅

| 项 | 规格 |
|----|------|
| 图鉴规模 | **12 张**——rare / epic / legendary 各 4 张 |
| 共享性 | **两人共享一本图鉴**（`stickers` 表 `sticker_key` 全局唯一，重复解锁忽略） |
| 序号分配 | 从"本地已知该档解锁数 + 1"起**逐个试到该档上限**，撞上已占用序号就试下一个（2026-09-16 修）。旧实现"数出 next 就直接插"，一旦本地状态滞后于数据库（冷启动时列表还没拉回来、双端同刻开奖）就会撞唯一约束并**静默放弃** —— 那次开奖白开（线上 2026-08-08 有 5 次开奖、之后 8 天一张贴纸都没解锁）。逐个试还顺带补上历史空洞、让"是否集齐"以插入结果为准 |
| 解锁失败可见 | 解锁出错不再只写 console，会提示"图鉴解锁没成功，下次开出同档会自动补上"（下次开同档隐藏款时按计数自然补上） |
| 展示 | 分档网格：已解锁显示彩色贴纸 + 名称 + 解锁日期 + 「新」角标，未解锁显示灰色剪影 + `???` |
| 进度 | 分档图例「稀有 x/4 · 史诗 x/4 · 传说 x/4」+ 总进度条 + `x / 12` |
| 专属短句 | 点击已解锁贴纸弹出该贴纸的专属文案（故事卡） |
| 集齐庆祝 | 金色彩带 + Toast，通过 localStorage 持久化**只庆祝一次** |
| 红点提示 | 有新解锁未查看时图鉴入口显示红点 |
| 引导 | 贴纸轻晃引导，localStorage 记录，最多 3 次 |

> 当前贴纸**仅用于收集展示**，不能贴到待办上做装饰（代码中无相关实现）。

### 5.10 心里话（阅后即焚）`F-10` ✅

| 项 | 规格 |
|----|------|
| 入口 | 纪念日面板底部「留一句心里话」 |
| 写信 | 全屏信纸场景，≤ 200 字，≥ 180 字时字数提示变暖橙色；按天存 localStorage 草稿 |
| 发送 | 逐条独立保存，**不覆盖**历史留言 |
| 接收 | 顶栏信封图标（有未读时显现 + 呼吸光晕）；**不弹系统通知** |
| 阅读 | 全屏呈现，单击「阅」翻到下一条——**当前这条随即焚毁**（标记已读 + 软删除） |
| 珍藏 | 最后一条需**长按 600ms**，文字化作爱心飞向顶栏桃心后焚毁 |
| 数据 | `daily_notes` 表，软删除 |

**产品价值**：这是整个产品里情感浓度最高的功能。它刻意**不保留记录**——正因为它会消失，说出口的门槛才低。

### 5.11 纪念日 `F-11` ⚠️

| 项 | 规格 |
|----|------|
| 顶栏常驻 | 「在一起 N 天」，数字 1.5 秒滚动动画 |
| 面板内容 | 相识天数（正计时）+ 结婚纪念日倒计时 + 两人生日倒计时 |
| 倒计时逻辑 | 年度循环计算；当天显示「🎉 就是今天」，否则「还有 N 天」 |
| 配色 | 顶栏桃心按时段变色——清晨柔樱粉（6–9 点）、白天品牌樱粉、深夜深梅紫（22 点–次日 6 点），在 `<head>` 内联脚本中预上色避免跳变 |

> ⚠️ **缺口**：纪念日、婚期、两个生日的日期**全部硬编码**在 `anniversary.js` 中，**没有配置界面**。换用户使用必然失效（见 §9 T-03）。

### 5.12 爱心反应 `F-12` ✅

| 项 | 规格 |
|----|------|
| 反应类型 | 当前仅启用 ❤️（heart）；OK / 点赞等仅保留历史数据的渲染兼容 |
| 作用对象 | **仅已完成**的待办（未完成则入口不显示） |
| 操作 | 长按菜单入口，可连点切换（贴/取消） |
| 去重 | `UNIQUE (todo_id, user_id, emoji)`，同一人对同一条同一表情只存一次 |
| 实时反馈 | 对方贴图时本地播放胶囊脉冲动画（`pulseTodoOnRemoteReaction`），**不弹系统通知** |

### 5.13 完成庆祝与动效 `F-13` ✅

| 场景 | 表现 |
|------|------|
| 普通完成 | 随机鼓励文案 Toast + 爱心形状彩带（`shapeFromPath`）+ `playDing` 音效 + 震动 `[30, 20, 30]` |
| 隐藏款完成 | 专属文案 + 稀有度对应庆祝（见 F-08）+ 卡片光环 |
| 登录光晕 | 顶栏爱心闪 N 下（见 F-07） |
| 首屏 | 爱心开屏动画（1.2 秒，快进快出） |
| 加载 | 顶部进度条 + 列表骨架屏 |
| 无障碍 | 全部动效受 `prefers-reduced-motion` 守卫 |

**主题**：当前**仅启用樱白粉（v2.7.61 起，主色 **#e884a8**、底色 **#fffbfc** 冷粉白）单主题**。此前色系演进：v2.7.48 焦糖玫瑰 #c2410c → v2.7.59 提亮 #d34c14 → v2.7.60 再提亮 #e8561d（暖橘珊瑚，用户反馈「无论调暗调亮都显脏、像护眼模式」），v2.7.61 整体更换为低饱和日系樱白粉色系，彻底消除黄感。早期曾有 5 套主题切换（薄荷绿等），后在一次重构中移除切换 UI（样式变量体系保留 `data-theme` 钩子，可恢复）。**暂未启用暗色模式**（color token 命名已预留 `--color-bg-dark` 等，参见 design-tokens.md §2.2）。

### 5.14 通知 `F-14` ✅

| 项 | 规格 |
|----|------|
| 技术方案 | `@capacitor/local-notifications`（纯 Web 环境下 no-op） |
| 触发时机 | 仅 Realtime 收到对方的**新增待办** / **完成待办** |
| 通知内容 | 「清单有新待办」/「待办已完成」+ 对方名字 + 内容前 40 字 |
| 前置条件 | **App 不在前台**时才弹（`appInForeground` 守卫），前台时改由 UI 反馈 |
| 不推送 | 心里话、爱心反应、贴纸解锁、隐藏款揭晓——均走应用内反馈，不打扰 |

**设计原则**：通知极其克制。只推"事务性"事件（加了事、做完了），不推"情感性"事件（留言、贴纸）——后者应该由用户主动发现，而不是被推送打断。

### 5.15 发布与更新 `F-15` ✅

双通道更新机制，是本项目工程上最成熟的部分：

| 通道 | 适用 | 机制 | 生效时机 |
|------|------|------|---------|
| **热更新**（默认） | 仅改 `public/` 下前端资源 | `scripts/release.mjs` → 打包 zip → 上传 Storage bucket `app_updates`（`releases/<ver>.zip`）→ 写 `app_versions` 表 → 客户端 @capgo `CapacitorUpdater` 下载并 reload。触发方式二选一：本地直接跑脚本，或走 CD 工作流（见「关键细节」末条） | 下次冷启动 |
| **APK 更新** | 改了原生层（插件/权限/配置） | `scripts/release-apk.mjs` → 上传 `apks/youai-<ver>.apk` → 写 `app_native_versions` 表（含 sha256）→ 客户端 `apk-update.js` 比对 → 原生 `ApkInstaller` 下载（sha256 校验）+ 唤起系统安装器 | 用户确认后安装 |

**关键细节**：
- **版本号是构建期注入的，仓库不持有它**（2026-09-14 改）：`public/index.html` 的
  `<meta name="app-version">` / `<meta name="shell-version">` 恒为占位值 `0.0.0`；
  热更新由 `release.mjs` 在**暂存副本**上注入（发布对工作区零改动），
  APK 由 `release-apk.mjs` 在 cap sync 后注入（`app-version` = 线上最新 web 版本、`shell-version` = 本次壳版本）。
  占位值是 fail-safe：漏注入只会让 App 多重启一次，不会造成「本地偏高 → 永远收不到更新」
- APK 版本比对读 `App.getInfo()` 的**真实 versionName**，避免被热更新快照误导（meta 仅作浏览器/调试兜底）
- 强更机制：`is_force_update=true` 或本地版本 < `min_supported_version` 时，更新面板不可关闭
- 检查时机：冷启动 + 前台切回（60 秒节流）
- **下线 vs 回滚是两件事**（2026-09-16 更正）：
  - **下线**（`node scripts/rollback.mjs <版本号>`，把 `app_versions.enabled` 置 `false`）：效果是**止损**——
    还没更新的设备 + 新装机不会再拿到这个版本。但它**不能把已经更新的设备退回去**：客户端判定更新的
    依据是「服务端版本 ≤ 本地版本 → 无更新」，那些设备本地版本已经更高了，会一直停在上面。
    旧文档把这条写成"回滚"，且 `rollback.mjs` 打印的"客户端会落到上一条 enabled 版本"是错的（已更正）。
  - **真回滚**（`node scripts/release.mjs <新版本号> --from-git <旧 ref>`）：内容取自旧 ref 的 `public/`、
    版本号用更高的新号、meta 注入到新号上 —— 于是已更新的设备会正常下载并退回旧代码。
    这是唯一能把线上退回去的手段；`--dry-run` 可先预演（会打印"本次将回退掉哪些改动"）。
  - 自动兜底只有一种：插件 `resetWhenUpdate:true` 连续启动崩溃 3 次自动回退——**只覆盖崩溃类故障**，
    UI/文案/逻辑类问题（不崩溃）不触发。
- **版本号守卫的基准是"历史上出现过的最大的号"，不只是 enabled**（2026-09-16 改）：已下线的版本号也算"用过"
  （如 2.7.65 是回滚演练留下的 `enabled=false` 行）。只跟 enabled 行比会放行一个曾经下发过的号，
  导致那批设备永远收不到更新。`assertNewerThanLatest()` 现在扫全表取最大值。
- 桌面 APK **固定文件名** `~/Desktop/有爱.apk`，每次覆盖，不产生带时间戳的历史文件
- **CD 通道**（`.github/workflows/release-web.yml`）：手动触发（`workflow_dispatch`），工序为「预演出报告 → 静默期 + `production` 环境审批 → 执行 `release.mjs` → `verify-release.mjs` 回读校验」。四道门：手动触发 + `confirm` 逐字确认版本号 + `assertNewerThanLatest` 版本守卫 + 环境审批人。写生产库属高风险动作，**刻意不做 push 自动发版**
- **回读校验**（`scripts/verify-release.mjs`，只读）：发布后必须验证「交付物可用」而非「脚本没报错」——版本行 `enabled`、Storage 对象可下载、包内 `index.html` 的 meta 与版本号一致，三项缺一即视为发布失败
- **仓库不欠任何 meta 提交**（2026-09-14 改）：版本注入只发生在打包产物上（热更新的暂存副本 / APK 的 android assets），
  `release.mjs` 与 `release-apk.mjs` 都不再改写 `public/index.html`。旧设计下「CI 发布后仓库 meta 落后线上、
  需要补一个 PR（还会触发约 11 分钟模拟器 CI）」的已知限制**已随设计消除**

> 📌 **血泪教训（已固化为流程）**：发布前必须先查线上最新版本号，新版本号必须语义化大于线上值。曾因未查版本直接发 2.0.1（线上已是 2.2.4）导致用户端判定"无更新"，白等半天。

### 5.16 PWA 与离线 `F-16` ⚠️

| 项 | 规格 |
|----|------|
| 安装 | `manifest.webmanifest`，`display: standalone`，竖屏，192/512 + maskable 图标，主题色 `#ffe4e6` → 可添加到手机主屏 |
| Service Worker | 版本 `v15`，缓存名 `todo-shell-v15` |
| 缓存策略 | 原生环境（localhost https）**完全 bypass 不缓存**；浏览器端：导航 network-first 回退缓存、静态资源 cache-first、跨域（Supabase）全走网络 |
| 离线能力 | 浏览器 PWA 可离线打开 app shell，但**数据仍需联网** |

> ⚠️ **部分实现**：`addTodo` 已支持离线暂存补发（断网时乐观显示「待同步」待办，联网后自动补发）。完成 / 编辑 / 删除离线时仍提示「当前离线」，无法暂存（见 §9 T-06）。
>
> ✅ **补发幂等**（2026-09-16）：重放加了防重入 —— `online` 事件可能连续触发两次，而队列项要等
> `createTodo` 回来才出队，并发重放会把同一条补发成**两条重复待办**（表现为"删掉一条后列表里还有
> 同文案的另一条"）。隐藏款也会在补发时补做图鉴解锁（旧实现里离线开出的隐藏款永远不解锁）。

### 5.17 已建未接入的能力 `F-17` 🔒

| 字段 | 现状 |
|------|------|
| `todos.nudge_by` | 「轻轻提醒」标记字段 + `db.setNudge` 已实现，**无 UI 入口**。初衷是克制地标记"这条我很关注" |

---

## 6. 数据模型

### 6.1 表清单

| 表 | 用途 | Realtime |
|----|------|----------|
| `profiles` | 用户档案（用户名、显示名、最后在线、打开计数） | ✅ |
| `todos` | 待办主表 | ✅ |
| `daily_notes` | 心里话（阅后即焚） | ✅ |
| `reactions` | 待办表情反应 | ✅ |
| `stickers` | 贴纸图鉴 | ✅ |
| `app_versions` | 热更新版本清单 | ❌ |
| `app_native_versions` | 原生 APK 版本清单 | ❌ |

### 6.2 `todos` 表字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | UUID | 主键 |
| `text` | TEXT | 待办内容，≤ 200 字（DB CHECK 约束） |
| `completed` | BOOLEAN | 完成状态 |
| `created_by` | UUID | 创建者 |
| `created_at` | TIMESTAMPTZ | 创建时间 |
| `completed_by` | UUID | 完成者 |
| `completed_at` | TIMESTAMPTZ | 完成时间 |
| `nudge_by` | UUID | 轻轻提醒标记人 🔒 无 UI |
| `image_path` | TEXT | 单图 URL（**旧字段，只读兼容**） |
| `image_paths` | JSONB | 多图 URL 数组（**当前使用**） |
| `completed_note` | TEXT | 完成备注，≤ 100 字 |
| `pinned` | BOOLEAN | 是否置顶（true=置顶，显示在列表最上方） |
| `deleted_at` | TIMESTAMPTZ | 软删除标记 |
| `rarity` | TEXT | 稀有度：common / rare / epic / legendary |
| `rarity_seen` | BOOLEAN | 隐藏款是否已被对方看过 |

**完整性约束**：`completed_consistent` CHECK——未完成则 `completed_by`/`completed_at` 必须为 NULL；已完成则二者必须非空。

### 6.3 安全模型（RLS）

| 角色 | `todos` / `reactions` / `stickers` / `daily_notes` | `profiles` |
|------|---------------------------------------------------|-----------|
| 未登录（anon） | 完全拒绝 | 完全拒绝 |
| 已登录（authenticated） | 可读写**全部** | 可读全部，只能改自己 |

> **为什么已登录用户能读写所有人的数据？** 因为这是"双人共享清单"——两个账号必须能看到并操作同一份数据。安全性由"**只有 2 个固定账号能注册**"这一前提保证。这是一个明确的架构权衡：**牺牲数据隔离，换取零权限复杂度**。一旦开放注册，此模型必须推翻重做。
> ⚠️ 该前提在**生产**项目由 `disable_signup=true` 保证（2026-09-16 实测确认）；**测试项目**是 `false`（它的数据是一次性的，可接受，但别把这个差异套到生产）。

> 📌 **2026-09-16 收紧**：`profiles` 的 SELECT 原为 `USING (true)` 且**没写 `TO ...`** ⇒ 对 PUBLIC（含 anon）开放。
> 而 anon key 是公开的（硬编码在 `public/js/supabase.js`，随 APK/网页分发），等于任何人可列举两人的
> 用户名 / 显示名 / 最后在线时间 / 打开计数。现统一限定为 `authenticated`（App 里读 `profiles` 只发生在登录之后：
> `db.js listProfiles()/updateLastSeen()`，故对功能无影响）。
> **同一时期发现的事故**：测试项目的 `profiles` 表 RLS 被手工关掉（仓库 `schema.sql` 里一直是开的），
> 被 Supabase 安全顾问报 CRITICAL `rls_disabled_in_public` —— 修复见 `supabase/migration-rls-hardening.sql`，
> 防复发见 §8.2 与 `app-e2e/scripts/check-rls.mjs`。

### 6.4 存储

| Bucket | 用途 | 公开性 |
|--------|------|--------|
| `todo-attachments` | 待办图片附件 | 公开读，写权限由 RLS 限制为 authenticated |
| `app_updates` | 热更新 zip (`releases/`)、APK (`apks/`) | 签名的私有 URL 访问 |

---

## 7. 技术架构

### 7.1 架构图

```
┌──────────────────────────────────────────┐
│              客户端（单页应用）             │
│  ┌────────────────────────────────────┐  │
│  │  原生 HTML/CSS/ES Module（无框架、   │  │
│  │  无构建步骤）                        │  │
│  │  app.js · db.js · realtime.js ...   │  │
│  └──────────────┬─────────────────────┘  │
│  ┌──────────────┴─────────────────────┐  │
│  │  Capacitor 壳（Android）            │  │
│  │  LocalNotifications · SystemBars    │  │
│  │  SplashScreen · CapacitorUpdater    │  │
│  │  ApkInstaller（自定义原生插件）      │  │
│  └──────────────┬─────────────────────┘  │
└─────────────────┼────────────────────────┘
                  │ HTTPS + WSS
┌─────────────────▼────────────────────────┐
│         Supabase（免费层，无自建服务器）    │
│  ┌──────────┬──────────┬───────────────┐ │
│  │   Auth   │ Postgres │   Realtime    │ │
│  │ （登录） │ （RLS）  │  （WebSocket） │ │
│  ├──────────┴──────────┴───────────────┤ │
│  │  Storage（图片附件 / 更新包分发）      │ │
│  └─────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

### 7.2 技术选型

| 层 | 技术 | 选择理由 |
|----|------|---------|
| 前端 | 原生 HTML + CSS + ES Module | 零构建、零框架，改完即发，热更新包体积极小 |
| 客户端 SDK | `supabase-js`（esbuild 打包到 `public/js/vendor/`） | 直连 BaaS，无需自建后端 |
| 状态管理 | 自研 `state.js`（Set 多订阅，返回 unsubscribe） | 单页应用，状态简单，无需引入框架 |
| 原生壳 | Capacitor | 一套 Web 代码出 Android APK |
| 后端 | Supabase（PostgreSQL + Auth + Realtime + Storage） | 免费层足够 2 人使用，免去服务器运维 |
| 分发 | Supabase Storage + 自建更新协议 | 零成本实现热更新与 APK 分发 |
| 本地服务 | `scripts/serve.mjs`（Node 内置 http，零依赖） | 开发调试 |
| 测试 | Playwright（headless Chromium）+ Python 脚本 | 双账号 E2E、视觉回归 |

### 7.3 前端模块划分

| 模块 | 职责 |
|------|------|
| `app.js` | 主页入口与核心逻辑（约 60KB，最大的模块） |
| `db.js` | 数据访问层（Supabase 查询封装） |
| `realtime.js` | Realtime 订阅 + Presence |
| `state.js` | 状态管理与订阅 |
| `transforms.js` | 数据字段转换（DB ↔ 前端模型） |
| `auth.js` / `login.js` | 认证与登录页 |
| `messages.js` | 心里话（阅后即焚） |
| `sticker-book.js` / `blindbox.js` | 贴纸图鉴 / 隐藏款盲盒 |
| `anniversary.js` | 纪念日 |
| `reactions.js` | 爱心反应 |
| `lightbox.js` / `image-utils.js` | 图片全屏预览 / 压缩上传 |
| `confetti-effects.js` | 各类庆祝动效 |
| `action-sheet.js` | 长按菜单 |
| `notify.js` | 本地通知 |
| `update.js` / `apk-update.js` | 热更新 / APK 更新 |
| `theme.js` / `toast.js` / `utils.js` | 主题、轻提示、工具 |

---

## 8. 非功能需求

### 8.1 性能

| 指标 | 目标 | 实现保障 |
|------|------|---------|
| 实时同步延迟 | < 1 秒 | Supabase Realtime WebSocket |
| 操作响应 | 乐观更新，本地即时反馈 | 失败才回滚 |
| 首屏 | 骨架屏 + 1.2 秒开屏动画 | 避免白屏感知 |
| 图片体积 | 短边 ≤ 1280，JPEG 0.92 | 上传前客户端压缩 |

### 8.2 安全

- 密码由 Supabase Auth 托管哈希存储，不明文
- 所有数据表启用 RLS；未登录角色完全拒绝访问业务数据
  - ⚠️ 这句话从 2026-09-16 起**是机器判定的**，不再只是声明：`node app-e2e/scripts/check-rls.mjs`
    用 anon 视角探针（payload 必然违反外键、**不写任何数据**）验证每张业务表 —— 42501=策略拦下了，
    约束错误=RLS 没开。它由 `admin/scripts/init-test-env.mjs` 第 ④ 步调用 ⇒ 属 **CI required job**。
  - 血泪：测试项目的 `profiles` 曾被手工关掉 RLS 而无人发现（顾问报 `rls_disabled_in_public`）。
    根因是「建表靠粘贴一次、之后没有回读校验」；对策即是上面这条 + `scripts/test_rls_migration.mjs`
    （无凭据、用 PGlite 把 `supabase/migration-rls-hardening.sql` 真跑一遍）。
- **函数执行权限同样收紧**（2026-09-16 补）：PostgreSQL 默认把函数 EXECUTE 授予 `PUBLIC`，
  于是 public schema 里每个 `SECURITY DEFINER` 函数默认「拿到公开 anon key 的任何人可调用」。
  实测踩中三个：`create_test_user`（**测试项目已删除**，anon 可调 ⇒ 任何人可建账号）、
  `increment_login_count` / `consume_login_count`（anon 可调 ⇒ 未登录就能写 `profiles`）。
  已按 `supabase/migration-rpc-execute-hardening.sql` 收回 PUBLIC/anon，只留 authenticated + service_role。
  ⚠️ 两个必须记住的坑：`revoke ... from anon` 是**空动作**（权限来自 PUBLIC），必须 `from public, anon` 并补
  `grant ... to authenticated`；反过来只写 `GRANT ... TO authenticated` 也不移除 PUBLIC 的默认授权。
  机器判定：`app-e2e/scripts/check-rls.mjs` 会断言「anon 调 RPC 必须被拒」+「暴露面白名单」，
  由 `admin/scripts/init-test-env.mjs` 第 ④ 步带进 CI required job。
- Storage 写入权限限制为 authenticated
- APK 更新包带 **sha256 校验**，防止篡改
- 修改他人数据的 RPC（`consume_login_count`）用 `SECURITY DEFINER` + `search_path` 锁定，避免权限提升
- 前端输入有长度约束 + DB CHECK 双重校验

### 8.3 隐私与合规

- 数据完全存储在自有 Supabase 项目，无第三方分析/广告 SDK
- 心里话采用阅后即焚，**服务端不保留可读副本**（软删除后仅留标记）
- 图片存储于公开 bucket，安全性依赖路径不可猜测（含 todo UUID）
- 无用户行为追踪、无数据统计埋点

> 📌 **待补充**：目前**没有任何使用数据埋点**，因此无法回答"哪个功能最常用""两人一天互动几次"这类问题。若要做产品决策，需先补基础指标（见 §11）。

### 8.4 可靠性

| 风险 | 应对 |
|------|------|
| 误删数据 | 软删除（已实现，但缺恢复 UI） |
| 更新包导致崩溃 | `resetWhenUpdate:true`，连续崩溃 3 次自动回退 |
| 版本下发错误 | `app_versions.enabled=false` 可即时下线 |
| 网络断开 | 离线提示条 + 自动重连 |
| **Supabase 免费层暂停** | ⚠️ 免费项目长时间无访问会被暂停，需定期唤醒（见 §9 T-09） |

### 8.5 兼容性

- **主平台**：Android（Capacitor APK）
- **次平台**：iOS Safari / Android Chrome（PWA，可装主屏）
- 竖屏布局，适配移动端单手操作
- 无障碍：`prefers-reduced-motion` 守卫、aria 标签、语义化按钮

---

## 9. 已知限制与技术债

> 这一节是本文档最有价值的部分之一。以下均为**核对代码后确认**的真实缺口，不含推测性抱怨。

| ID | 问题 | 影响 | 严重度 |
|----|------|------|--------|
| **T-03** | **纪念日日期硬编码**——相识日/婚期/生日写死在 `anniversary.js`，无配置界面 | 产品无法给其他人使用；日期错了要改代码重新发版 | 🟡 中 |
| **T-06** | **离线写入仅支持添加**——`addTodo` 已支持离线暂存补发，完成/编辑/删除仍不支持离线 | 弱网场景下除「添加」外其余操作体验断层 | 🟡 中 |
| **T-07** | **`nudge_by` 字段空转**——数据层已实现，无 UI 入口 | 已投入的开发工作未产生用户价值 | 🟢 低 |
| **T-08** | **贴纸仅供收集**——不能贴到待办上装饰 | 收集动力依赖纯成就感，无二次使用场景 | 🟢 低 |
| **T-09** | **Supabase 免费层会被暂停**——长时间无访问项目自动暂停 | 需要定期访问唤醒，否则某天打开直接失败 | 🟡 中 |
| **T-10** | **账号完全固定**——增加第三个用户需要改代码 + 手工建号 | 产品无法规模化，但这是当前定位下可接受的取舍 | 🟢 低（设计取舍） |
| **T-11** | **无搜索 / 筛选 / 分类 / 截止日期 / 提醒** | 待办数量增长后（> 50 条）检索效率下降 | 🟡 中 |
| **T-12** | **`app.js` 约 60KB 单文件**——虽已拆出多个模块，主文件仍偏大 | 长期维护成本上升 | 🟢 低 |

> ✅ **已解决（2.7.45 / 2.7.46 / 2.7.48）**，已移出上表：T-01 回收站无恢复 UI、T-02 删除无撤销、T-04 贴纸解锁时机不一致、T-05 图片删除不清理 Storage。

---

## 10. 迭代历程

从立项到 `2.7.49`，产品经历了八个阶段。理解这条脉络，才能理解为什么产品是现在这个形态。

| 阶段 | 时间 | 关键变化 | 产品含义 |
|------|------|---------|---------|
| **V1 立项** | 2026-07 中 | Express + Socket.IO 自建服务，双人待办 CRUD | 验证"两个人共享一份清单"这个最小假设 |
| **V2 架构重构** | 2026-07 底 | 重构为 PWA + Supabase 直连，**去掉自建服务器** | 运维成本归零，实时同步交给 Realtime |
| **视觉个性化** | 2026-08 初 | 5 套主题、完成动画、PWA 安装 | 从"能用"走向"愿意用" |
| **情感化功能期** | 2026-08 中 | 图片附件、完成备注、心里话、爱心反应、隐藏款盲盒、贴纸图鉴、纪念日、本地通知 | **产品定位发生质变**：从效率工具变成情感载体 |
| **体验打磨与工程化** | 2026-08 末 – 09 | 玫瑰暖色系视觉升级、暗色贺卡、写信页重设计、图鉴视觉升级、多图支持、模块拆分重构、样式 Token 化、APK 强更机制 | 把快速迭代欠下的工程质量补回来 |
| **安全网补全** | 2026-09 上 | 回收站 + 删除撤销、图片清理、退后台节电、通知修复、离线添加待办 MVP、图鉴入口冷启动修复 | 补齐防误删、离线、性能三类安全网 |

**观察到的规律**：
1. **功能是长出来的，不是规划出来的**——心里话、盲盒、图鉴都不在最初的 PRD 里，是在使用过程中发现"需要一点惊喜"才加的。
2. **情感化功能上线后，产品才真正活起来**——纯待办阶段的使用频率受限于"有多少事要记"，加入盲盒和心里话后，打开动机从"有事要做"扩展为"想看看今天有什么"。
3. **工程债是快速迭代的必然代价，必须专门还**——2026-09 的整轮重构（模块拆分、监听器治理、样式 Token 化）就是在补这笔账。

---

## 11. 后续演进建议

> ⚠️ 以下为**建议**，非既定计划，需与产品所有者确认优先级后执行。

### 11.1 建议优先处理（补齐安全网）

| 优先级 | 事项 | 理由 |
|--------|------|------|
| P0 | **补基础使用埋点** | 目前完全没有数据，所有产品决策只能靠感觉。至少要有：日活、日新增待办数、日完成数、功能使用分布 |
| P1 | **纪念日可配置** | 解除硬编码，是产品能被第二对情侣使用的前提 |

### 11.2 建议中期考虑（提升天花板）

| 优先级 | 事项 | 理由 |
|--------|------|------|
| P2 | **待办搜索 + 已完成归档** | 待办积累到一定量级后的必然需求 |
| P2 | **截止日期 + 提醒** | 从"记下来"升级到"别忘掉"，但会显著提高复杂度，需谨慎 |
| P2 | **贴纸可用于装饰待办** | 让图鉴从"收集"变成"可用"，形成正循环 |
| P2 | **数据导出** | 用户对自有数据的掌控感，也是防丢的最后一道保险 |

### 11.3 建议暂缓（可能伤害现有体验）

| 事项 | 暂缓理由 |
|------|---------|
| 多人协作 / 开放注册 | 会推翻现有的"全共享 RLS"安全模型，且违背"只属于两个人"的定位 |
| 任务指派 / 优先级 / 子任务 | 会让产品变成"另一个滴答清单"，丧失极简优势 |
| 聊天 IM 功能 | 心里话的克制（阅后即焚）正是它的价值所在，做成聊天会稀释 |
| 引入前端框架 | 当前零构建 + 小体积是热更新体验的关键优势，收益不抵成本 |

### 11.4 关键指标建议（North Star）

若补充埋点，建议关注：

| 层级 | 指标 | 说明 |
|------|------|------|
| **北极星** | **双端同日活跃天数 / 周期天数** | 两人在同一天都打开并使用——这是"在一起做事"的最直接度量 |
| 驱动指标 | 日均新增待办数、日均完成数、完成率 | 反映协作活跃度 |
| 驱动指标 | 隐藏款开出次数、图鉴解锁进度 | 反映惊喜机制的拉动效果 |
| 驱动指标 | 心里话发送条数 | 反映情感连接强度 |
| 健康指标 | 7 日留存、连续使用天数 | 反映产品是否真的被依赖 |
| 健康指标 | 同步失败率、崩溃率、更新成功率 | 反映工程质量 |

---

## 12. 附录

### 12.1 常用命令

```bash
# 本地开发（默认 http://localhost:3000）
node scripts/serve.mjs

# 发布热更新（纯前端改动；发布前务必先查线上最新版本号）
node scripts/release.mjs <版本号> --notes "<说明>"
node scripts/release.mjs <版本号> --dry-run     # 只打包校验，不上传不写库

# 发布后回读校验（只读：版本行 enabled + Storage 对象可下载 + 包内 meta 一致）
node scripts/verify-release.mjs [版本号]        # 不传版本号 = 校验线上最新

# 生产数据备份（只读导出到 backups/，含完整行可恢复；铁律一：删除前必须先备份）
node scripts/backup-tables.mjs --reason "<原因>"
node scripts/backup-tables.mjs --reason "<原因>" --dry-run   # 只报告行数，不落盘

# 安全自检（2026-09-16）：RLS 是否真的生效 + 加固 SQL 是否可用
node app-e2e/scripts/check-rls.mjs        # anon 视角探针，只读不写；RLS 没开时打印修复指引
node scripts/test_rls_migration.mjs       # 用 PGlite 真 Postgres 跑加固 SQL（无需凭据，进 CI）
# 修复用 SQL：supabase/migration-rls-hardening.sql（Dashboard → SQL Editor 整份粘贴 → Run，幂等）

# 远程发布（GitHub Actions CD：预演 → 审批 → 发布 → 回读校验）
gh workflow run release-web.yml -f version=<版本号> -f notes="<说明>" -f dry_run=true
# 预演通过后正式发：-f dry_run=false -f confirm=<版本号>

# 发布 APK（原生层改动；产物固定覆盖 ~/Desktop/有爱.apk）
node scripts/release-apk.mjs <版本号>

# 重新打包 supabase-js（升级 SDK 版本后）
npm run bundle:supabase
```

### 12.2 测试脚本

| 脚本 | 用途 |
|------|------|
| `scripts/test_blindbox.py` | 盲盒概率验证（约 15% 隐藏款）、图鉴 12 格、稀有度视觉截图；**强制开奖钩子下的完整链路**（提示合并/不互顶、序号递增与撞车自愈、集齐、完成时撤销按钮、提示串行）；**双账号对方端揭晓**（第二个账号 `e2e-beta` 收到「（某某 开出的）」提示 + 回标 + 共享图鉴同步） |
| `scripts/test_trash.py` | 回收站 + 删除撤销 E2E |
| `scripts/test_offline.py` | 离线添加待办 → 待同步 → 联网补发 E2E（含"补发只有一条"与"删除后不会自己冒回列表"两条回归断言） |
| `scripts/test_compress.mjs` | 图片压缩策略验证（短边 1280 阈值） |
| `scripts/test_pinch.mjs` | lightbox 双指缩放（mock Supabase，零生产写入） |
| `scripts/test_sticker_wiggle.mjs` | 贴纸轻晃引导逻辑（上限 3 次） |
| `scripts/test_rls_migration.mjs` | RLS 加固迁移（`supabase/migration-rls-hardening.sql`）：与仓库 SQL 逐字一致 + 在 PGlite（真 Postgres/WASM）里复现洞 → 修复 → 幂等；**不需要凭据** |
| `app-e2e/scripts/check-rls.mjs` | 真实测试库的**安全自检**：表 RLS + RPC 执行权限 + PostgREST 暴露面白名单（anon 探针，不写数据）；由 `admin/scripts/init-test-env.mjs` 第 ④ 步调用 ⇒ CI required job 里拦住 |
| `scripts/test_rpc_migration.mjs` | 函数权限加固迁移（`supabase/migration-rpc-execute-hardening.sql`）：PGlite 真 Postgres 里断言 anon 收干净 / authenticated 仍可用 / 注册触发器链路完好 / 幂等；**不需要凭据** |

### 12.3 术语表

| 术语 | 含义 |
|------|------|
| 隐藏款 | 添加待办时 15% 概率触发的稀有待办，分 rare / epic / legendary 三档 |
| 图鉴 | 收集隐藏款解锁的贴纸集合，两人共享一本，共 12 张 |
| 心里话 | 阅后即焚的悄悄话，看完即销毁 |
| 软删除 | 只标记 `deleted_at` 不物理删除的删除方式 |
| 热更新 | 只更新前端资源的发布方式，用户无需重装 App |
| 壳更新 | 需要重装 APK 的原生层更新 |
| RLS | Row Level Security，PostgreSQL 行级安全策略 |
| BaaS | Backend as a Service，后端即服务（本项目指 Supabase） |

### 12.4 相关文档

| 文档 | 说明 |
|------|------|
| `AGENTS.md` | 项目铁律与开发规范（**必读**） |
| `supabase/schema.sql` | 生产数据库 schema 副本 |

---

## 文档维护说明

- **本文档描述现状**。任何功能变更（新增、修改、移除）都应同步更新本文档，并递增文档版本号。
- **发现代码与文档不符时，以代码为准**，并立即修正文档——这正是本次重写要解决的问题。
- **§9 技术债表格是活的**：解决问题后请移出表格，新增问题请补入。

---

**文档版本**：v2.3 ｜ **撰写日期**：2026-09-07 ｜ **基于代码版本**：2.7.49 / 壳 2.1.28
