# E2E 测试环境变量（复制为 .env.test 并填入真实值；该文件已 gitignore）
#
# 用途：**APP 通道（app-e2e / Appium，Node）与 Web 通道（scripts/test_*.py，Playwright）
# 共用这一个文件** —— 两者连的是同一个独立 Supabase 测试项目（物理隔离生产库，铁律一）。
#
# 键位说明（少一个就可能「本地能过、CI 挂」，2026-09-14 批次 C 踩过）：
#   E2E_TEST_USERNAME  Web 通道用：App 的登录框收的是**用户名**（如「小宝宝」），
#                      scripts/e2e_common.py 的 login() 直接把它填进 #username。
#   E2E_TEST_EMAIL     APP 通道用：app-e2e/utils/test-data.js 按邮箱反查用户名。
#                      两个键都留：删掉哪个都会让某一条通道在 CI 上拿不到凭证。
#   CI 侧对应 GitHub Secrets 同名；.github/workflows/e2e-app.yml 与 e2e-web-full.yml
#   生成的文件与本地这份**同形**（这是刻意的：环境不同形是「本地绿/CI 红」的主因）。

E2E_SUPABASE_URL=https://your-test-project.supabase.co
E2E_SUPABASE_ANON_KEY=your-anon-key
E2E_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
E2E_TEST_USERNAME=test-user
E2E_TEST_EMAIL=test-user@example.com
E2E_TEST_PASSWORD=your-test-password
